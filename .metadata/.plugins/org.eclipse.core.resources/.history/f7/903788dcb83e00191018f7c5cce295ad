package Sorts;

import java.util.Random;

public class Shuffle {
	
	public static void shuffle(File[] a) {
		Random random = new Random();
		for(int i=0; i<a.length; i++) {
			int rand = random.nextInt(i+1);
			File swap = a[rand];
			a[rand] = a[i];
			a[i] = swap;
		}
	}
}
