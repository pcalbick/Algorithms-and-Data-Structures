package Stack;

import java.util.Iterator;

public class GenericStackLinkedList<Item> implements Iterable<Item>{
	
	private Node first = null;
	
	private class Node {
		Item item;
		Node next;
	}
	
	public void push(Item item) {
		Node old = first;
		first = new Node();
		first.item = item;
		first.next = old;
	}
	
	public Item pop() {
		Item item = first.item;
		first = first.next;
		return item;
	}
	
	private class GenericIterator<Item> implements Iterator<Item> {
		private Node index;
		
		public Item next() {
			index = index.next;
			return index.item;
		}
		
		public boolean hasNext() {
			return index.next != null;
		}
	}
	
	public class Item {
		String s;
	}
}
